#include "holberton.h"

/**
 * print_alphabet - prints alphabet in lowercase.
 *
 * Return: Always 0.
 */

void print_alphabet(void)
{

char al;

for (al = 'a'; al <= 'z'; al++)
{

_putchar(al);
}
return (0);
}
