#include <stdio.h>

/**
 * main - prints the first 50 Fibonacci numbers.
 *
 * Return: Always 0.
 */

int main(void)
{
long int a = 1, b = 2, c, d;
for (c = 1; c <= 50; ++c)
{
if (a != 20365011074)
{
printf("%ld, ", a);
}
else
{
printf("%ld, ", a);
}
d = a + b;
a = b;
b = d;
}
return (0);
}
