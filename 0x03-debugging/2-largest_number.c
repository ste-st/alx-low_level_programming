#include "holberton.h"

/**
 * largest_number - returns the lasgest of 3 numbers
 *@n: int
 *@m: int
 *@a: int
 * Return: largest.
 */

int largest_number(int n, int m, int a)
{
int l;

if (n >= m && n >= a)
{
l = a;
}
else if (m >= n && n >= a)
{
l = b;
else
{
l = a;
}
return (l);
}
